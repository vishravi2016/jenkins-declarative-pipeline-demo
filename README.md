# Node.js CI/CD Demo App

Simple Express application used for Jenkins CI/CD pipeline training.

## Run locally
npm install
npm start

## Run tests
npm test

## Build Docker image
docker build -t node-app:latest .
docker run -p 3000:3000 node-app:latest
"# jenkins-declarative-pipeline-demo" 
